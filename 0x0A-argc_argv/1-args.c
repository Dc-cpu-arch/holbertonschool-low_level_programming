#include "holberton.h"

/**
 * main - returns the number of arguments entered to it -integer value-
 *@argc: count the arguments
 *@argv: array of pointers
 *Return: allways 0
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	int i;

	if (argc == 1)
	{
		putchar ('0');
		putchar ('\n');
	}

	else
	{
		for (i = 1; i < argc; i++)
		{
		}

	printf("%d\n", i = i - 1);
	}


	return (0);

}
