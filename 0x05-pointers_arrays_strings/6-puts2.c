#include "holberton.h"

/**
 * puts2 - prints even integers.
 * @str: pointer to string to print.
 * Return:Null.
 */

void puts2(char *str)
{
	int i;

	while (str[i]
		{
			if (i % 2 == 0)
			{
				_putchar(str[i];
			}
					i++;
		}
			_putchar('\n');
		}


}
